import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.helpconnect.app',
  appName: 'Teste Sem Capacitor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
